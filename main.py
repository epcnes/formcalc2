# packages used just in case
#!pip install googlesearch-python; !pip install requests; !pip install bs4, !pip install wikipedia

import wikipedia; from bs4 import BeautifulSoup

formula = str(input("Enter formula name: "))